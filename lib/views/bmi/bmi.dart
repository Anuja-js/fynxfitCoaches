import 'package:flutter/material.dart';

class BMIScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(child: Text("BMI Calculation", style: TextStyle(fontSize: 24)));
  }
}
